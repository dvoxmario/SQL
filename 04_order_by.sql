SELECT * FROM users ORDER BY age ASC; /* con esta sentencia traemos todos los datos de la tabla users y los ordernamos por edad de forma ASCENDENTE*/

SELECT * FROM users ORDER BY age desc; /*con esta sentencia traemos todo los datos de la tabla users y lo ordenamos por edad de forma DESCENDENTE

SELECT * FROM users WHERE name='ejemplo' ORDER BY age; con esta sentencia le damos mas detalle a nuestro query trauendo todos los datos de la 
tabla users ordenando traer los datos de la columna name y ordenando por edad. */
