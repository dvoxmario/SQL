
SELECT * FROM users; /* con esta sentencia llamamos a todos los datos de la tabla users.*/

SELECT name FROM users;  /* con esta sentencia llamamos solo la columna name de la tabla users.*/

SELECT user_id, name FROM users;  /* con esta sentencia llamamos dos columnas de la tabla users.*/




